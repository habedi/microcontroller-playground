############################################################################
# experiments/pico-face/Make.defs
#
# Reached through the apps/external symlink, which is why the path below is
# $(APPDIR)/external rather than the name of this directory.
#
############################################################################

ifneq ($(CONFIG_PICO_FACE),)
CONFIGURED_APPS += $(APPDIR)/external
endif
