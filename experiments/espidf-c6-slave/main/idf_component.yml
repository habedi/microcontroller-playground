dependencies:
  # The coprocessor half of ESP-Hosted. Built for the esp32c6 target, this is
  # the firmware the C6 on the ESP32-P4 function EV board should be running.
  # Host and coprocessor come from the same version on purpose, since a
  # mismatch is the leading suspect for the link not working.
  espressif/esp_hosted: "^3.0.6"
